version: "3.8"

services:
  focal:
    image: quay.io/stackstorm/packagingrunner
    extends:
      file: docker-compose.override.yml
      service: suite-compose
    environment:
      - BUILDNODE=focalbuild
      - TESTNODE=focaltest
      - ST2_PACKAGES=st2
    links:
      - focalbuild
      - focaltest
      - rabbitmq
      - mongodb
      - redis

  jammy:
    image: quay.io/stackstorm/packagingrunner
    extends:
      file: docker-compose.override.yml
      service: suite-compose
    environment:
      - BUILDNODE=jammybuild
      - TESTNODE=jammytest
      - ST2_PACKAGES=st2
    links:
      - jammybuild
      - jammytest
      - rabbitmq
      - mongodb
      - redis

  el8:
    image: quay.io/stackstorm/packagingrunner
    extends:
      file: docker-compose.override.yml
      service: suite-compose
    environment:
      - BUILDNODE=rockylinux8build
      - TESTNODE=rockylinux8test
      - ST2_PACKAGES=st2
    links:
      - rockylinux8build
      - rockylinux8test
      - rabbitmq
      - mongodb
      - redis

  el9:
    image: quay.io/stackstorm/packagingrunner
    extends:
      file: docker-compose.override.yml
      service: suite-compose
    environment:
      - BUILDNODE=rockylinux9build
      - TESTNODE=rockylinux9test
      - ST2_PACKAGES=st2
    links:
      - rockylinux9build
      - rockylinux9test
      - rabbitmq
      - mongodb
      - redis

  ## Package build nodes
  #
  focalbuild:
    image: stackstorm/packagingbuild:focal
    extends:
      file: docker-compose.override.yml
      service: volumes-compose

  jammybuild:
    image: stackstorm/packagingbuild:jammy
    extends:
      file: docker-compose.override.yml
      service: volumes-compose

  rockylinux8build:
    image: amitgupta369/packagingbuild:el8
    extends:
      file: docker-compose.override.yml
      service: volumes-compose

  rockylinux9build:
    image: amitgupta369/packagingbuild:el9
    extends:
      file: docker-compose.override.yml
      service: volumes-compose

  ## Package testing nodes
  #
  focaltest:
    image: stackstorm/packagingtest:focal-systemd
    privileged: true
    cgroup: host
    extends:
      file: docker-compose.override.yml
      service: volumes-compose
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup

  jammytest:
    image: stackstorm/packagingtest:jammy-systemd
    privileged: true
    cgroup: host
    extends:
      file: docker-compose.override.yml
      service: volumes-compose
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup

  rockylinux8test:
    image: amitgupta369/packagingtest:el8
    privileged: true
    cgroup: host
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp:unconfined
    extends:
      file: docker-compose.override.yml
      service: volumes-compose
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup

  rockylinux9test:
    image: amitgupta369/packagingtest:el9
    privileged: true
    cgroup: host
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp:unconfined
    extends:
      file: docker-compose.override.yml
      service: volumes-compose
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup

  rabbitmq:
    image: rabbitmq:4.0-management
    privileged: true
    hostname: rabbit
    ports:
      - "15672:15672"  # managment plugin port
      - "5672:5672"

  mongodb:
    image: mongo:8.0
    ports:
      - "27017:27017"

  redis:
    image: redis:7.4
    ports:
      - "6379:6379"
